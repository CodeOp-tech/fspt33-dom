// target my h1 element
const h1Element = document.querySelector(".title");

// update the text content to uppercase
h1Element.textContent = h1Element.textContent.toUpperCase();

// target my h2 element
const h2Element = document.querySelector(".sub-title");

// update the text content to uppercase
h2Element.textContent = h2Element.textContent.toUpperCase();
